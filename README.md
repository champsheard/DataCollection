# Info For Project


### Data graphing software for python:
**Matplotlib → “manual control”**
**Seaborn → “simple + pretty”** 
**Plotly → “interactive”**


### ETL
**Extract, Transform, Load**


### Structured Vs unstructured data
For this project I am going to go with structured data because it will organize it better and allow the AI to query it.

### AI models
As far as AI models go, I will probably end up either using a trained small model eg qwen, or a hevier cloud model. I would like to keep everything local, but am unsure of the processing power needed because right now the server is just a RPI 4